# ML Project — Infrastructure

This repository contains a minimal, ready-to-use infrastructure for a the machine learning 1 project.

## How to Run the Project

To run the project, follow these steps:

1. **Clone the Repository**:
   ```bash
   git clone https://github.com/afkirmohamed11/ProjetML1UCBL1.git
   ```

2. **Navigate to the Project Directory**:
   ```bash
   cd ProjetML1UCBL1
   ```

3. **Start the Docker Containers**:
   Ensure Docker and Docker Compose are installed on your system. Then, run:
   ```bash
   docker-compose up --build
   ```

4. **Access the Application**:
   - Frontend: Open your browser and navigate to `http://localhost:3000`
   - Backend: API documentation is available at `http://localhost:8080/docs`

5. **Stop the Containers**:
   To stop the running containers, use:
   ```bash
   docker-compose down
   ```
